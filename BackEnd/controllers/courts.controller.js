class CourtsController {
    createCourt(req, res) {
        res.status(404).send("Work In Progress!");
    }

    getCourtById(req, res) {
        res.status(404).send("Work In Progress!");
    }

    updateCourt(req, res) {
        res.status(404).send("Work In Progress!");
    }
}

const courtsController = new CourtsController();
export default courtsController;